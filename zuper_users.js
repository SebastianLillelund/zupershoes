class User {
    constructor(firstName, lastName, address, userName, userPassword) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.address = address;
        this.userName = userName;
        this.userPassword = userPassword;
    }
}
