class User {
    constructor(firstName, lastName, userName, userPassword) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.userName = userName;
        this.address = address;
        this.userPassword = userPassword;
    }
}
